<!-- GENERATED BY `npm run bench` — DO NOT EDIT BY HAND. -->
<!-- Hand-written analysis belongs in benchmarks.md; this file is only what the current build scores. -->

# Current benchmark results — v0.14.6

Generated 2026-09-12 from commit `50362f2`.

Every number on this page came from one run of `npm run bench` against the snapshot and
flags named below. Nothing here is carried forward from an earlier version; if a row is
missing, that suite did not run, and the reason is at the bottom.

## Provenance

| | |
|---|---|
| Version | 0.14.6 |
| Commit | `50362f2` |
| Date | 2026-09-12 |
| Retrieval flags | `AWM_RERANK2=1 AWM_RERANK_WINDOW=query AWM_RERANK_TAGS=1` |
| Snapshot | `tests/realstore-eval/snapshot/store.db` |
| Engrams in snapshot | **29,853** total · **11,262** retrievable (active, not retracted, not superseded) |
| Decay clock pinned to | 2026-08-24T09:27:21.841Z |

> **Which engram count to quote.** Recall can only ever return the *retrievable*
> population, so that is the honest denominator for a retrieval score. The larger
> total counts staged, retracted and superseded rows the ranker never considers.
> Quote one, say which, and use the same one everywhere.

## Retrieval

| Suite | probes | s@1 | s@5 | MRR | abstention | p50 | p90 |
|---|---|---|---|---|---|---|---|
| Identifier queries (real store) | 300 | **92.7%** | 96.7% | 94.6% | 90.0% | 563 ms | 710 ms |
| Topic queries (real store) | 450 | **92.0%** | 96.7% | 94.2% | 90.0% | 504 ms | 605 ms |

- **Identifier queries (real store)** — of the golds retrieved, 99.7% actually contain the answer; net token economics +519 per recall.

## Temporal cues

This suite is a comparison, not a score: it asks whether phrasing a query with a time
cue ("last week", "in March") helps, and what a working date filter would be worth.

- No temporal cue: **90.1%** s@1 — the baseline.
- Oracle week-filter: **96.0%** — the ceiling a working filter approaches, 5.9pp above baseline.
- Per-phrasing detail in `bench-runs/0.14.6-2026-09-12/temporal.log`. Every cue phrasing currently scores *below* the no-cue baseline, so the cue is not yet being used as a filter.

## Not regenerated by this run

- **Memory Gauntlet (end-to-end ablation).** Lives in the memory-working-agent repo, costs real API spend per arm, and every probe flips between identical runs. Making the probes deterministic is the prerequisite; more reps will not fix it.
- **Production retrieval cost audit.** Reads local Claude Code transcripts, so it measures one operator's usage rather than a property of the release. Re-run deliberately with scripts/measure-claude-vs-awm.ts.
- **LoCoMo.** Retired in 0.13.x: 115-char passages against a real store's ~2,000, one-shot seeding so decay and Hebbian signals contribute nothing, and it rewards indiscriminate retention. Kept only as the record of a tuning decision.

## Reproduce

```bash
npm run bench             # the retrieval fixtures above
npm run bench -- --all    # plus the local challenge suites
```

Raw stdout for every suite is archived under `bench-runs/0.14.6-2026-09-12/`.
